<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="api.server.payment.infrastructure.mapper.PaymentQueryMapper">

	<select id="selectRiskInfo">
		SELECT merchant_id AS merchantId, risk_level AS riskLevel, reason
		FROM risk_info
		WHERE merchant_id = #{merchantId}
	</select>

	<select id="selectTransact" >
		SELECT *
		FROM transactions
		WHERE trans_id = #{transId}
	</select>

	<select id="selectOrgTransact">
		SELECT *
		FROM transactions
		WHERE org_trans_id = #{orgTransId}
	</select>

	<select id="selectOrgPartTransact">
		SELECT *
		FROM transactions_part
		WHERE org_trans_id = #{orgTransId}
	</select>

	<insert id="insertTransact">
		INSERT INTO transactions (trans_id, merchant_id, status, currency, amount, auth_code, org_trans_id, risk_info)
		VALUES (#{transId}, #{merchantId}, #{status}, #{currency}, #{amount}, #{authCode}, #{orgTransId}, #{riskInfo})
	</insert>

	<update id="updateTransact" >
		UPDATE transactions
		SET status = #{status}, currency = #{currency}, amount = #{amount}, auth_code = #{authCode}
		WHERE trans_id = #{transId}
	</update>

	<update id="updateOrgTransact">
		UPDATE transactions
		SET org_trans_id = #{orgTransId}, status = #{status}
		WHERE trans_id = #{transId}
	</update>

	<update id="updateEDIRefund">
		UPDATE transactions SET status = 'REFUND' WHERE trans_id = #{transId}
	</update>

	<update id="updateEDICancel">
		UPDATE transactions SET status = 'CANCEL' WHERE trans_id = #{transId}
	</update>

	<update id="updateEDIPartCancel">
		UPDATE transactions SET status = 'PART_CANCEL' WHERE trans_id = #{transId}
	</update>

	<select id="checkAuthTransact" resultType="int">
		SELECT COUNT(*)
		FROM transactions
		WHERE trans_id = #{transId} AND auth_code = #{authCode}
	</select>


</mapper>
